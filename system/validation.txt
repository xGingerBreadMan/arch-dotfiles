2026-09-22: JSON files parse successfully. Internal asset/service symlinks resolve. Restore preview leaves files unchanged; apply replaces files while preserving original contents in backup; relative symlinks restored correctly. Secret-pattern review: one source-code false positive (changePassword command invoking passwd), no credential matches. This is a targeted review, not a guarantee that every arbitrary secret format can be detected.
